require('../helpers/install-xhr')
const lookups = require('../../utils/lookups')
const assert = require('assert')

// TODO: write tests after @pavel.mash create mocks for UB.ClientRepository

describe('Lookups', () => {
  describe('#init', () => {
    it('Shall create empty entities subscriptions', () => {
      assert()
    })
  })
  describe('#subscribe', () => {
    it('Shall load data on first subscription', () => {
      assert()
    })

    it('Shall not load data on repeat subscription', () => {})

    it('Shall load data on repeat subscription if adds attrs', () => {})
  })
  describe('#unsubscribe', () => {
    it('', () => {
      assert()
    })
  })
  describe('#get', () => {
    it('', () => {
      assert()
    })
  })
})
