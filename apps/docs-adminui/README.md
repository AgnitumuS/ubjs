> Warning!
> Now works only with yarn

Prerequisite: 
- Install dependencies `npm i`.
- `./init-db.sh`
- Dev mode `npm run dev`
- Build `npm run build`

Production version
- `ub`
- `http://localhost:8881/`
